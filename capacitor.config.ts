import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.lenzro.erp",
  appName: "Al-Shifa Herbal",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
